class CatsModel {
  String cat_details,
      cat_name,
      img_url,
      isAdded,
      isFeatured,
      username,
      doc_id,
      collection;

  CatsModel(this.cat_details, this.cat_name, this.img_url, this.isAdded,
      this.isFeatured, this.username, this.doc_id, this.collection);
}
